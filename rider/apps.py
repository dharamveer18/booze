from django.apps import AppConfig


class RiderConfig(AppConfig):
    """The delivery partner app (mobile-first web app at /rider/)."""

    name = "rider"
    verbose_name = "Booze delivery partner app"
